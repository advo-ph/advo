# ADVO Roadmap

Unified, forward-looking roadmap for the ADVO platform and the business that runs on top of it. Synthesizes signals from the Messenger archive (Apr–Jun 2026), the May 26 client meeting, and the shipped Codex `LandingPage` on `/` (`278a65a`).

Treat this as the **canonical roadmap**. Two existing sub-roadmaps stay where they are and are referenced from here:

- **Landing design** — see [/ROADMAP.md](../ROADMAP.md) (historical Stripe-audit cut). Public `/` is the shipped Codex `LandingPage`. Title/meta, Fourlinq proof, social URLs, and `landing-shell` on `/start` `/login` `/team` `/project/:slug` are on `main`.
- **Platform feature backlog** — see [FEATURES.md → Roadmap](FEATURES.md#roadmap) (Internal Library, Admin UX cleanup, monorepo).
- **Feature wiring audit** — see [WIRING-AUDIT.md](WIRING-AUDIT.md) (2026-06-20 end-to-end audit of every admin + client feature). The 🔴 cross-tenant data-leak bugs (**S1/S2/S3**) are **fixed + deployed** (`0e42f13`); B1/B2/B3 + several papercuts also fixed. **All security items S1–S4 are now fixed + deployed** — S4 (`9574820`) removed the `VITE_GITHUB_TOKEN`/`VITE_CLOUDFLARE_TOKEN` browser reads; the GitHub feed routes through the backend `github_event` cache. **Tier 3 leftover W1–W5 + R3–R4** (branding hydrate, Add Admin user, notify-rule label, dashboard activity, social stats, team reorder/order) is shipped on `lane/wiring`.

## Status snapshot

|                                          |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| ---------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Frontend**                             | advo.ph live, monorepo (`apps/web`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| **API**                                  | api.advo.ph live (`apps/api`, PM2 on VPS)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| **Active client engagements**            | Felici Gelato (in dev), Coffee Rush (in dev)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| **Shipped**                              | FourlinQ **website** (`fourlinq.ph`) — June 19, 2026. The FourlinQ *application* is a separate, still-open engagement — see **Largest open deal** below.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| **Latest platform work (2026-06-20)**    | Project Command Center (per-project hub) · contract red-flag review (now **AI-capable**, Claude + heuristic fallback) · Files/Drive pillar · Show-Client-Now expiring preview links + client request-from-Hub · email-on-new-lead · Deliverables CRUD · Tier-1/2 security fixes (incl. **S4 closed**) + DB FK migration · **Linear UI redesign** (admin + hub — Hanken Grotesk, dense tables, no mono) · **ADVO records calendar** — Phase 1 (month grid + derived deliverable/invoice/project events + manual event CRUD) **live**; **Phase 2** (content/social + contracts/MOA + PH compliance-deadline layers derive into the union; new `contract` table + CRUD at `/api/contracts`, migration `004`; compliance deadlines for BIR/SSS/PhilHealth/Pag-IBIG/DOLE generated from a sourced schedule `apps/api/src/data/compliance-deadlines.ts`, no table) **deployed to prod 2026-06-20** (migration `004` applied as app DB user, web rsynced, `advo-api` restarted). Compliance schedule is cited (RR 11-2018 / RA 11976 / agency circulars); weekend/holiday shifting + per-registration filtering not applied. Phase 3 (Google/ICS sync) decided as **two-way**. See [HANDOFF.md](HANDOFF.md). |
| **Plaud 07-30 fan-out CP1 (2026-07-31)** | **Expense ledger** (`005_expense` + `/api/expense` + Admin Finance Expenses; reimbursable ⇔ receipt present) · **client document upload** on project assets · **hub live preview iframe** when `preview_url` set · **client signed contracts** via `GET /api/contracts/mine`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| **Plaud 07-30 fan-out CP3 (2026-07-31)** | **AI / heuristic PM assist** (Claude `claude-opus-4-8` when `ANTHROPIC_API_KEY` set, else fallback): **Generate tasks** from meeting transcript → `POST /api/meeting/:id/generate-task` (1–8 deliverables + Suggested skill; UI on AdminMeetings + Command Center) · **Suggest timeline** → `POST /api/projects/:id/suggest-timeline` (phase/milestone plan, singular keys; `activity_log` `timeline_suggested`, response-primary) · **Client revision task** → `POST /api/projects/:id/revision-task` (deliverable "Client revision" + CONTRACTS.md 2-rounds/phase reminder; method `raw`\|`ai`). Remaining CP2/CP4 items (meetings/MoM, PM capacity, penalty, presentation, Vercel ops docs) tracked by workflow `plaud-fanout`. Deliverable verify already shipped (P7 / migration `007`). |
| **Plaud import + Ask + folder watch (2026-08-16)** | **Import** file id / share URL / praud Inbox. **Folder watch** every `PLAUD_POLL_SECOND` for ADVO-tagged or named files. **Generate tasks** Ask Plaud first (`/ask/v2/ask` + roster JSON, `method: ask`) then note parse / Claude / heuristic. Preview then confirm writes `deliverable.assignedTo`. Migrations `012`/`013`. Needs `PLAUD_TOKEN` on the box for watch + Ask. |
| **Largest open deal**                    | **FourlinQ Windows and Doors — Integrated HR & Operations Management Application.** Contract dated 08/11/2026, open as of 08/14/2026 — neither investment tier is initialed on the copy on file, so the tier is not yet selected. Tier 1 Core Attendance ₱45,000 / Tier 2 ADVANCED Integrated Management ₱70,000, 50% on signing + 50% on Project Sign-off, plus **₱3,000/month infrastructure fee** starting on final delivery and deployment. See [Active client status → FourlinQ](#fourlinq--largest-open-deal-application-contract-tier-not-yet-selected). |
| **Stalled**                              | Daj, Ms. Imee (Inventi), Tita Imee                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| **Passed/declined**                      | Personal Collection (already on Shopify)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| **Prospecting**                          | Medical City (David's cousin lead), dental clinic outreach (5K scraped leads)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |

## P0 — Revenue & contracts

Highest leverage: every signal in Messenger about money or scope points here. Without these, revisions are unbounded and downpayments don't cover the work.

| Item                                         | Why                                                                                                                                         | Effort | Status                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| -------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------- | ------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Revision limits in contract template**     | Fourlinq + Felici both ran open-ended revisions. Quote, David (Jun 19): _"we lowk shuldv specified pala sa contract ung revisions thingy"_. | S      | ✅ Draft clause in [CONTRACTS.md](CONTRACTS.md#policy-2--revisions) — **superseded 2026-08-19**: now 5 free rounds per deliverable, usable until Project Sign-off, with a 6-month tail for unused rounds. The old 2-rounds-per-phase/hourly-overage rule is retired. **Needs legal review before binding use.**                                                                                                                                                                                                                                                                                                                          |
| **Change-order process**                     | Once revisions are capped, scope changes need a paper trail. Email + signature OK to start; eventually a form in `/hub`.                    | S      | ✅ Draft clause in [CONTRACTS.md](CONTRACTS.md#policy-3--change-orders-aka-i-saw-this-on-another-site) (`791a039`). ✅ Hub form: client files scope + reason on `/hub` project (`POST /api/change-order`); team lists/quotes via GET/PATCH. Work still waits on quote + client confirm. Binding language still needs the lawyer. |
| **Proposal-to-contract pipeline**            | Felici signed because of a custom PDF. Currently every proposal is a one-off Canva file.                                                    | M      | ✅ Command Center **contract red-flag review** (`97b213a` / `fae49dd`) + **proposal generator** + **sent/opened/replied/signed tracker** at `/admin` → Proposals (`010_proposal`, `/api/proposal`). **AI generation shipped**: `POST /api/proposal` returns `method: ai` when `ANTHROPIC_API_KEY` is set — Claude writes the body copy from the lead's own scraped signals (digital / design / performance score, industry, budget) — and `method: template` when it is not, running the unchanged template fill. Clauses + money table are rendered by us, never by the model. `proposal.method` column via `014`. CONTRACTS.md drop-in clauses still need legal review before a signed engagement. |
| **Downpayment floor**                        | Fourlinq's 12k downpayment didn't cover the work (David: _"the 12k isnt enough as a downpayment"_).                                         | S      | ✅ Draft clause in [CONTRACTS.md](CONTRACTS.md#policy-1--payment-schedule) — **superseded 2026-08-19**: now a 50/50 milestone split (50% on commissioning, 50% on final delivery and sign-off). The 40%/₱30k floor is retired — at real tier sizes 50% of the smaller tier falls below ₱30k, so both could not hold. Needs legal review + applied to Coffee Rush before signing.                                                                                                                                                                                                                                                                                                         |
| **Project sign-off document** (migration `016`) | The contract makes "Project Sign-off" the trigger for final payment, the revision cutoff, and the start of the 6-month unused-revision window — but nothing in the platform records that a sign-off happened. | M | ✅ **Shipped** — migration `016`, `/api/project-signoff`, `/hub` sign card, `/admin → Projects → Sign-off`. **Acceptance:** a project can be signed off exactly once, storing signatory, signed-at, and the tier/scope snapshot, and the record is readable through the `{ data, error }` envelope on a `requireAuth` + `requireTeam` route. |
| **Recurring infrastructure fee** (migration `017`) | ₱3,000/month for FourlinQ and ₱3,000/month for Felici are contracted but tracked nowhere; the platform has no notion of a monthly charge that starts at go-live. | M | ✅ **Shipped** — migration `017`, `/api/recurring-fee`, `/admin → Finance → Recurring fees`. **Acceptance:** a project carries a monthly fee in integer centavos with a start date, and billing periods generate from that start date without double-charging a period already generated. |
| **Commission split 60/25/15** (migration `018`) | Prince's 2026-06-19 compensation structure — 60% devs / 25% staff / 15% company reserve, with staff further split 28% referral / 24% marketing / 24% accounting / 24% management — is a text note, not a system. | M | ✅ **Shipped** — migration `018`, `/api/commission`, `/admin → Finance → Commission`. **Acceptance:** a project's collected amount splits into dev / staff / company allocations in integer centavos whose sum equals the input exactly, with no rounding centavo created or lost. |
| **Deemed-approval notice** (migration `021`) | CONTRACTS.md Policy 3 is what makes the 5-round revision allowance finite: 15 business days of silence, then a formal **Notice of Pending Deemed Approval**, then 15 more. The policy also says skipping the notice **forfeits the whole mechanism** — so with nothing recording the delivery date, the notice, or its evidence, ADVO could not have claimed deemed approval even where it had earned it. | M | ✅ Shipped — `021` adds the clock, the notice + its mandatory `notice_reference`, and the human-recorded outcome. Deadlines are **derived, never stored**, following `deriveWindow`. Four DB CHECKs enforce the policy (notice must be producible; deemed approval requires a notice; a client who responded can never be deemed silent; a notice cannot predate delivery) — verified against a live DB, not just asserted. **PH holidays are not subtracted**, so every derived date is the EARLIEST possible and nothing acts on it automatically; a named human records the approval. 27 tests. |
| **Engage Philippine corporate/cyber lawyer** | All **nine** CONTRACTS.md policies need validation before use — and the reconciled terms already went to a client ahead of review, so this is now a live exposure rather than a precaution, and Prince explicitly flagged the need in Jun 2026.                                | M      | ⏳ **Lawyer still not engaged** — but the packet to send one is now written: [LEGAL-BRIEF.md](LEGAL-BRIEF.md), self-contained, all nine policies with their operative clause language quoted inline, 49 closed-form questions (answerable yes / no / yes-with-modification), the live exposure stated in the first screenful, the real commercial figures counsel needs to judge a payment clause, the unanswered RA 10173 outreach questions, and a bounded engagement ask that should return a fixed-fee quote rather than a conversation. Entity registration details are marked TODO, never guessed. Graded by `npm run bench:legal` (9/9). **Remaining work is human: pick a lawyer and send it.** Punch list source stays in [CONTRACTS.md](CONTRACTS.md#open-questions-for-the-legal-advisor).                                                                                                                                                                                                                                                                                                                                                                       |
| **PayMongo merchant-review disclosures** | PayMongo reviews the live site before approving a merchant, and the requirement list Prince sent 2026-08-21 names four public disclosures plus a business address and customer-service contact. No disclosure existed on advo.ph, so card and e-wallet acceptance was blocked at review. | S | 🟡 **Surfaces shipped** (`lane/compliance`) — `/terms`, `/privacy`, `/refund`, `/dispute` are public routes outside every `ProtectedRoute`, linked from the shared footer on every page, all four rendering merchant identity from the single `data/legal-identity.json`. **Blocked on Prince for the identity facts**: registered name, registration body, registration number, business address, and support phone are the literal string `TBD` and render as "Not yet published" rather than an invented value, so `npm run bench:paymongo` is **5/7** and stays red on `legal-identity-filled` + `legal-support-contact` until the DTI/SEC paperwork is transcribed. Filling that one file turns both green with no code change. |

## P1 — Lead generation & proposal automation

Currently bottlenecked: 5K scraped clinic leads but proposals are manual, so the pipeline can't fan out.

| Item                                                | Why                                                                                                                                                                                                 | Effort                                                                                                                                               |
| --------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------- |
| **Email-on-new-lead notification** ✅               | Leads come in via the contact form; you don't see them until logging into admin. Two test rows in DB are evidence nobody's monitoring it.                                                           | ✅ **Shipped** (`8bc719f`) — `POST /api/leads` emails all admins via `sendLeadNotificationEmail` (Resend SMTP when `RESEND_API_KEY` set, else logs). |
| **Import the ~5K scraped clinic leads into the DB** | The CSV + JSON from the Messenger archive (metro-manila clinics, with digital/design/perf scores) aren't loaded yet — the pipeline below assumes they are. Precursor to everything in this section. | ✅ **Importer shipped** — `npx tsx scripts/import-clinic-lead.ts` + `data/clinic-lead/sample.json`. Dedupes by email. The ~5K archive is a path argument when the Messenger dump is present; do not invent 5K rows. |
| **Clinic-scraper → proposal-PDF pipeline**          | Already have 5K leads with digital scores, design feedback, perf grades. Need: feed → AI-design proposal → send.                                                                                    | ✅ **Shipped** (`/admin` → Proposals → Generate). Those scraped scores are now parsed server-side (`lead-signal.service.ts`) and drive **AI body copy** when `ANTHROPIC_API_KEY` is set (`method: ai`); the template fill is the unchanged fallback (`method: template`). Both render CONTRACTS.md clauses verbatim into printable HTML. |
| **Proposal tracker**                                | Once you send 10+/month, need to know which clinics opened, replied, signed.                                                                                                                        | ✅ **Shipped** — `proposal` table + `/admin` → Proposals. Statuses: sent / opened / replied / signed. |
| **Targeting rule: zero/outdated systems only**      | Repeating signal: _"if a company has a system/website, we can't just offer them a new one"_ (Prince) — Personal Collection passed, AAPM has Inventi, etc.                                           | ✅ **Shipped** — `/admin` → Leads “Outdated only” filter (zero/outdated systems; skip Shopify/Inventi/etc.). |
| **Email campaign sender (mass send)**               | The last missing link in this section. Leads are imported, targeted, and a proposal can be generated — but nothing sends it, so the 5K-lead pipeline still cannot fan out. `email.service.ts` is transactional-only (one `send()`, errors swallowed to a log, single `noreply@advo.ph` transport shared with magic links). | ✅ **Shipped** — `campaign` / `campaign_recipient` / `email_suppression` (migration `015`) + `/api/campaign` + `/admin` → Campaigns. Separate outreach transport (`OUTREACH_SMTP_HOST` + `OUTREACH_FROM`); sending is **refused** when unset and never borrows the transactional mailer. Graded by `bench/roadmap/final/campaign.mjs` (17/17). **Needs the outreach subdomain + DNS, and the RA 10173 questions in [CONTRACTS.md](CONTRACTS.md#open-questions-for-the-legal-advisor) answered, before a real send.** The DNS half is enforced, not advised: `npm run outreach:preflight` resolves SPF/DKIM/DMARC live, refuses an outreach domain equal to the transactional one, records the verdict to `docs/outreach-preflight.json`, and the sender refuses while that verdict is failing, stale (>30d), or recorded for a different domain. Graded by `bench/roadmap/outreach-preflight/scoring.mjs` (8/8). |

### Acceptance — email campaign sender (v1)

Scope is deliberately **batch send + suppression**, not sequences. A follow-up sequence engine is a separate, later item.

| Must | Detail |
| --- | --- |
| **Separate sending identity** | Outreach sends from its own subdomain (e.g. `outreach.advo.ph`) via its own transport, configured independently of the transactional one. A reputation hit on cold outreach must not be able to stop a client magic-link from landing. |
| **Segment → campaign** | Select a lead segment (reuse the existing “Outdated only” targeting filter), attach a subject + body template or a generated proposal, and materialize one recipient row per lead at send time. |
| **Throttled send** | A configurable per-hour rate cap with the send resumable across an API restart. No unbounded `Promise.all` over 5K addresses. |
| **Unsubscribe + suppression** | Every outreach email carries a working one-click unsubscribe. An unsubscribed, hard-bounced, or complained address is permanently suppressed and is skipped by every future campaign, with no way to send to it from the UI. |
| **Bounce + complaint handling** | Hard bounce and spam complaint feed the suppression list on the first report. Repeated soft bounce increments a per-address counter and escalates to a `soft_bounce_limit` suppression at `SOFT_BOUNCE_LIMIT` (3). Shipped 2026-08-23, migration 020 — the endpoint is ready, but no ESP webhook calls it yet. |
| **Per-recipient status** | queued / sent / bounced / unsubscribed / complained, visible in admin, so a campaign's real delivery is legible without opening the ESP dashboard. |
| **Honest dry-run** | A campaign can be previewed and counted without sending, and the send path refuses to run when no outreach transport is configured — it must not silently log-and-succeed the way `email.service.ts` does today. |

**Not in v1:** multi-step sequences, reply detection, A/B subject testing, click tracking, inbox rotation.

**Legal:** the scraped clinic list is personal data under the PH Data Privacy Act (RA 10173). Add consent basis, retention, and unsubscribe obligations to the open-questions punch list in [CONTRACTS.md](CONTRACTS.md#open-questions-for-the-legal-advisor) for the same lawyer engagement already tracked in P0. Do not treat shipping the sender as clearance to send.

## P1 — Project management for multi-client throughput

Direct quote, Prince (May 6): _"we need a proper workflow or system na we'd be able to manage or handle multiple clients at once — specifically the workload on the developer side."_

| Item                                      | Why                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Effort                     |
| ----------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------- |
| **Capacity view in Admin**                | `AdminAvailability` tracks per-member time blocks (school/work/break/unavailable) and computes overlapping free time. **Audit finding:** what's missing is the _commitments_ side — projects-per-dev. `project_access` table already links team↔project, and the single-project endpoint (`GET /api/projects/:id`) returns the team. But the projects _list_ endpoint (`GET /api/projects`) does not include team data, so a per-member project count needs either (a) extending the list endpoint to include `team_member_ids[]` per project, or (b) a new `GET /api/team/:id/projects` endpoint. Then in AdminAvailability: show each member's active-project count beside their tab + a "capacity remaining" indicator. | ✅ `GET /api/projects` includes `teamMemberId[]`. Availability tabs show active-project count + remaining capacity (soft cap 3). |
| **Per-junior client assignment workflow** | Prince's pattern: 1 junior dev per client (Anthony / Au / Kenneth) under his supervision. Surface this in `AdminTeam` + `AdminProjects`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | ✅ `POST/DELETE /api/projects/:id/team` + Command Center Team panel (junior-filtered assign). Projects card still grants via `/access`. |
| **School/availability blackout calendar** | "pre-fi to finals szn" cost ~2 weeks of throughput. Track each member's school blocks so client timelines don't promise into them.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | ✅ Calendar "School blackout" layer expands school/unavailable weekly blocks onto the month grid. |

## P2 — Platform polish (UX / landing)

Mostly captured in the two sub-roadmaps; surfaced here so you don't lose them.

| Item                                                                            | Source                                              | Status                                                                                                                                                                        |
| ------------------------------------------------------------------------------- | --------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Portfolio proof cards (outcome metrics, before/after, system map)               | Codex stash                                         | ✅ Shipped `2360771` — case_study fallbacks + ProofMock; cards 2-col                                                                                                          |
| Full-screen mobile nav drawer                                                   | Codex stash                                         | ✅ Shipped `2360771` + `bc0ac03` (z-index fix). Numbered tap rows, a11y, escape, scroll lock                                                                                  |
| `/start` white landing chrome (`start-shell`)                                   | landing-follow                                      | ✅ `Start.tsx` mounts `landing-shell` — no Linear grid / `FloatingNav`                                                                                                        |
| `/login` white landing chrome (`login-shell`)                                   | landing-follow                                      | ✅ `Login.tsx` mounts `landing-shell` — Linear grid overlay removed                                                                                                           |
| `/team` white landing chrome (`team-shell`)                                     | landing-follow                                      | ✅ `Team.tsx` mounts `landing-shell` — no `FloatingNav` / old `Footer`                                                                                                        |
| `/project/:slug` white landing chrome (`project-shell`)                         | landing-follow                                      | ✅ `ProjectDetail.tsx` mounts `landing-shell`; remaining motion honors `reduceMotion`                                                                                         |
| Public landing footer 401 (admin-only `/api/settings` from anonymous footer)    | Open thread                                         | ✅ Shipped `a8a8cdc` — new `/api/settings/public` endpoint                                                                                                                    |
| Document title + OG match live landing (`title-meta`)                           | landing-follow                                      | ⚠️ **Deliberately reversed 2026-08-23** by `offer-truth`. Title/OG now read “ADVO. We digitalize it for you.” Prince asked for the mission line twice (08-19, 08-21); Angelo settled it on 08-23 in favour of shipping his copy verbatim. `landing-follow`'s `title-meta` check still asserts the June wording and is therefore **red on purpose** — the two benches cannot both be satisfied, and the founder's instruction wins. Retarget that check when `landing-follow` is next opened. |
| `landing-stripe-audit` viewport bench measures the deleted landing (`viewport-check`) | ship 2026-08-24 | ⚠️ **Stale, 22/63 green.** The bench was authored at `68d033f` against the old landing tree; `offer-truth` then rewrote `LandingPage.tsx` at `780485c` and did not re-aim it. It still asserts the old section headings (`serviceHeadingVisible`, `proofHeadingVisible`) and the old footer copy, so 41 checks fail against a page that is live and correct on prod. **Not to be fixed by editing the bench until someone decides what the new landing must guarantee** — re-authoring 41 assertions to agree with whatever the page currently renders would turn the bench into a mirror. One genuine defect it did catch was fixed at ship time: the rewritten nav had dropped `aria-controls` and the drawer `id` the old one carried. |
| Landing testimonials are real proof only (`proof-copy`)                         | landing-follow                                      | ✅ Invented names removed. Proof block is Fourlinq (`fourlinq.ph`, shipped June 19, 2026) only                                                                               |
| Landing social icons are real URLs (`social-wire`)                              | landing-follow                                      | ✅ `.landing-social` uses `GET /api/settings/public` + the same Facebook/Instagram/LinkedIn/mailto defaults as `Footer.tsx`                                                     |
| Hub.tsx hardcoded "Client" badge → use actual role                              | Open thread                                         | ✅ Shipped `383f90b`                                                                                                                                                          |
| Admin: empty-state CTAs                                                         | Open thread                                         | ✅ Shipped `383f90b` (Projects, Clients, Notifications)                                                                                                                       |
| Reduced-motion guards on all landing animations                                 | Codex stash                                         | ✅ Shipped on `LandingPage` + `landing-shell` (`reduceMotion` / `prefers-reduced-motion`). The unguarded Stripe leftovers (ContactCTA / TechTicker / InfrastructureDiagram) were never `/` and are now deleted. |
| Mobile viewport audit (no horizontal overflow at 360 / 390 / 768 / 1280 / 1440) | Codex stash                                         | ✅ `bench/roadmap/roadmap-remain/viewport-site.mjs` — 10/10 source + live at all five widths (`ADVO_LANDING_URL=http://127.0.0.1:<vite>/`). `bench:landing` is no longer Stripe-only: it was retargeted to the shipped surface and its `repoRoot` was `C:\C:\…` on Windows, so it silently scored 0/15 for every check regardless of the code. Now 17/17. |
| Strip "Why Go Digital" / generic FAQ → product-system framing                   | Codex stash                                         | ✅ The generic copy lived only in components `/` never rendered (`WhyDigital` / `FAQ` / `ContactCTA` / `Hero` / `ServiceTiers` / `ProcessSteps` / `TechTicker` / `InfrastructureDiagram` / `Footer` / `PortfolioGrid`) — all deleted. The shipped `LandingPage` FAQ is now website-vs-system / client hub / admin console / self-hosted VPS stack / hardware / after-launch, under "Questions before we build". |
| Footer system-continuity copy + oversized wordmark                              | Codex stash                                         | ✅ One shared `landing/landing-footer.tsx` for `/` and every `landing-shell` route: system-continuity lede, "Start the system" CTA, system-keyed columns, oversized wordmark (`data-viewport-check="footer-wordmark"`). No overflow at 360–1440. |
| No published price on `/` (`no-public-price`, `quotation-cta`)                   | Prince, 08-21 9:06pm                                | ✅ **Shipped** — the engagement grid names Project / Retainer / Hourly / Enterprise and routes each to a quotation request. Every peso figure is gone from `LandingPage.tsx` (rate card, "starting at", the mock invoice stat). `bench:offer` 9/9. |
| Work section reads the portfolio database (`work-from-portfolio`)                | Prince, 08-21 9:19pm                                | ✅ **Shipped** — `#work` renders `portfolio_project` through the already-public `GET /api/content/portfolio` (new read-only [`usePortfolio.ts`](../apps/web/src/hooks/usePortfolio.ts); no new endpoint). Large per-project screenshot, description clamped to 120 chars on a word boundary, link to the live site. The static tool rail and the duplicated Customer-Stories rail are deleted. **Empty table renders nothing.** |
| Tagline is the founder's mission line (`tagline-is-the-mission-line`)             | Prince, 08-19 + 08-21; Angelo 08-23                 | ✅ **Shipped** — hero, `<title>`, and OG all read "We digitalize it for you."; the vision statement carries the manifesto slot. Reverses the `title-meta` row above — see that row. |
| Landing visual system pass (`bench:visual`)                                      | Prince, 08-15 + 08-21 ("make it like runway.com ui")| ✅ **Shipped 8/8** — one `:root` palette (10 hex, ceiling 12), an eight-step type scale (ceiling 10 distinct `font-size` values), one `--landing-section-rhythm` token for every section, kickers cut to two, media leading. Graded on the reference's *properties*, never on resemblance — no asset, class name, or stylesheet was copied. |
| No dead code under `landing/`                                                   | Lane web                                            | ✅ `landing/` is exactly what a live route mounts: `LandingPage` (`/`), `landing-footer` + `landing-shell` (shell routes), `FloatingNav` (`/hub`), `PortfolioCard` (proof unit under test). Orphaned `.interactive-surface` / marquee + blob keyframes removed with them. Guarded by `no-dead-landing-module` in `bench:landing`. |
| Mobile drawer behaviour covered by a test, not by hand                          | Lane web                                            | ✅ `apps/web/src/test/mobile-nav-drawer.test.ts` — Escape close, non-Escape keys ignored, body scroll lock + restore, close on an external route change, close on its own link nav. |
| Internal Library at `/admin/library`                                            | [FEATURES.md](FEATURES.md#internal-library) | ✅ Lane admin — `/admin` → Library. `library_item` + `/api/library` (website/prompt/module/asset/doc). VPS file store still a follow-up. |
| Admin: modal → page for high-field-count CRUD (Projects, Clients)               | [FEATURES.md](FEATURES.md#admin-ux-cleanup)         | ✅ Lane admin — Projects + Clients create/edit are full pages, not dialogs                                                                                                      |
| Admin: hide experimental scrapers behind a "Tools" submenu                      | [FEATURES.md](FEATURES.md#admin-ux-cleanup)         | ✅ Lane admin — Tools group is collapsed (`toolsExpanded`); Brand/FB scrapers expand on demand                                                                                    |
| Capacity view in Admin (per-member project assignments + remaining capacity)    | This doc, P1                                        | ✅ `GET /api/projects` returns `teamMemberId[]`; Availability shows count + remaining capacity                                                                                 |
| Pretty preview route `advo.ph/p/:token`                                         | [HANDOFF.md](HANDOFF.md) (Show-Client-Now)          | ✅ Lane admin — frontend `/p/:token` branded gate then 302 via `GET /api/preview/:token`.                                                                                       |
| here.now fresh-deploy path (instant ephemeral preview)                          | [HANDOFF.md](HANDOFF.md) (Show-Client-Now)          | ⏳ Deferred — the original "instant temp deploy" ask. Current expiring-link approach is host-agnostic + works today; here.now needs an API key + per-project build artifacts. |
| Cloudflare Pages fresh-deploy path + artifact intake | the here.now row, unblocked from the other end | ✅ Shipped — `POST /api/projects/:id/preview-artifact` finally WRITES the artifact directory that `previewArtifactDir()` had only ever named, and a third `cloudflare` adapter deploys it to Pages. Path validation refuses traversal rather than sanitizing it; the artifact swaps in atomically. Chosen over here.now because the credential is self-issuable. **Still unverified end-to-end** — a live call returns Cloudflare's `9106: Authentication failed`, proving the endpoint but not the body; `provider-credential-live` stays RED until a real token runs it. 18 tests. |

### Acceptance — web-aug parcel (PayMongo compliance + offer truth)

Parcelled 2026-08-23 from the 08-15/08-21 Messenger threads; see [HANDOFF.md](HANDOFF.md#2026-08-23--web-aug-parcel-paymongo-compliance--offer-truth-2-lanes-not-yet-built). Both benches are committed **red** (`6ddb931`) and neither lane has been run.

| Item | Why | Effort | Status |
| --- | --- | --- | --- |
| **PayMongo disclosure routes** (`/terms`, `/privacy`, `/refund`, `/dispute`) | PayMongo reviews the live site before approving a merchant and names six required disclosures. None exist. Prince, 08-21: _"sir can u put all this to advo website"_. | M | ⏳ Lane `compliance`. **Acceptance:** each disclosure is a signed-out-reachable route registered outside `ProtectedRoute`, linked from `landing-footer.tsx`, rendering from one shared identity module — `npm run bench:paymongo` 6/7. |
| **Merchant identity of record** (`data/legal-identity.json`) | The registration number, registered business address, and support contact are not anywhere in this repo, and PayMongo requires all three. | S | ⏳ **Blocked on Prince**, not on code. `legal-identity-filled` stays red by design; the lane is forbidden from inventing a registration number. |
| **Remove published pricing** | Prince, 08-21: _"remove the price boi we dont put the pricing on the website, its always get a quotation"_. The landing publishes ₱60,000 / ₱80,000/mo / ₱800/hr. | S | ⏳ Lane `offer`. **Acceptance:** no peso figure, "starting at", `/mo` or `/hr` on the landing; the four engagement shapes survive as quotation requests — `npm run bench:offer`. Requires re-authoring `bench:landing`'s `engagement-cta`, which currently asserts the opposite. |
| **Work section reads the portfolio database** | Prince, 08-21: _"keep only the section for the websites that we've already created … large screenshot images, get them from the advo portfolio database … short and concise and simple descs only"_. `LandingPage.tsx` is fully static and the work rail is four hardcoded cards over `/landing/rw/*.jpg` stock. | M | ⏳ Lane `offer`. **Acceptance:** work cards render per-project screenshots sourced from `portfolio_project` with descriptions ≤120 chars, and render nothing rather than invented work when the table is empty. Needs a public read route the API does not have yet. |

**Excluded from the parcel** — design-blocked, do not fan these out:

- **Tagline → "We digitalize it for you."** Asked for twice by Prince (08-19, 08-21). Angelo objected in-thread that it reads software-only when ADVO also ships hardware, and [VISION.md](VISION.md) agrees. It would also revert the shipped `title-meta` row above, which deliberately dropped "We Digitalize It For You". Founder decision.
- **runway.com UI direction.** A visual reference, not acceptance criteria — no instrument can be authored.
- **Food / Medical / Education industry sections.** Blocked on the AI customer-journey videos Prince said he was still producing.


## Infra & Ops

Operational items running live prod — none of this is tracked elsewhere despite advo.ph + api.advo.ph + Postgres all being live.

| Item                                                 | Why                                                                                                                                                                                                                                                                                                                           | Effort                                                                      |
| ---------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------- |
| **VPS migration — API → company VPS**                | Planned move of the ADVO API to the company VPS; keep the deploy portable (no host-specific assumptions) so cutover is a git-pull + env swap.                                                                                                                                                                                 | M — env/secrets move, DNS, smoke                                            |
| **Activate AI paths in prod**                        | Contract review, meeting → tasks, timeline suggestion, revision-note polish, and **proposal body copy** all run Claude when `ANTHROPIC_API_KEY` is set, else heuristic/template/raw; prod has no key, so live paths are fallback-only.                                                                                        | S — add `ANTHROPIC_API_KEY` to VPS `apps/api/.env` + `pm2 restart advo-api`. **Confirmed still missing 2026-08-19**: `GET /api/health` reports `config.isAnthropicKeyConfigured: false`. |
| **Decommission or rebuild the orphaned LLM service** | ✅ **Deleted.** Vertex/Gemini `brand-analysis` route + service removed. `GET /api/brand-analysis` 404s. Claude stays for contract/PM assist. | ✅ Deleted (`lane/ops`) |
| **Monitoring / error tracking / backups**            | Nightly `pg_dump` is documented in [SETUP.md](SETUP.md) + [`apps/api/backup.sh`](apps/api/backup.sh) (cron 3am, 14-day retention). ✅ **Error capture + uptime ping wired** (`lane/resilience`): `GET /api/health` is the ping target — `status` is API-own liveness (db), `isDegraded` + `degradedReason` carry background-work health, `plaud.*` carries poller state, `error.*` carries a bounded in-process ring of redacted, stack-free messages. Secrets appear as booleans only; the endpoint is public. Zero new dependency. | ✅ Done (`lane/resilience`) — no external error service                     |

## Open test-coverage gaps

Behaviors that ship but have no automated test. Listed here so they don't get lost between sync-docs runs.

| Behavior                                                                      | Risk                                                                                                                                                                                                                                                                                                                                                                                                                                           | Effort                                      |
| ----------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------- |
| ✅ `GET /api/settings/public` (added in `a8a8cdc`)                            | Covered in `api-wiring.test.ts`: anonymous GET, `{ data, error }` envelope, allowlisted keys only (`social_links`, `brand_name`, `team_order`).                                                                                                                                                                                                                                                                                              | ✅ Done — anonymous-GET test                  |
| ✅ Role-based post-login redirect (admin → `/admin`, client → `/hub`)         | Extracted to [destination.ts](apps/web/src/lib/destination.ts); covered by [destination.test.ts](apps/web/src/test/destination.test.ts). `Login.tsx` calls `destinationFor(role, explicitRedirect)`.                                                                                                                                                                                                                                           | ✅ Shipped — 5 unit tests                    |
| ✅ Portfolio proof card fallback rendering                                    | `getProof()` fallbacks covered in `proof-card.test.ts` (render-tree): missing case study, description/techStack, camelCase fields, snake_case aliases + overview/challenge/solution.                                                                                                                                                                                                                                                           | ✅ Done — render-tree test                    |
| 🟢 Mobile drawer interactions (escape close, scroll lock, route-change close) | A11y-critical but currently only verified by hand.                                                                                                                                                                                                                                                                                                                                                                                             | M — playwright e2e                          |
| ✅ `DELETE /api/projects/:id/assets/:assetId` (Files pillar, `bdf1a8b`)       | Covered in `api-wiring.test.ts`: wrong-project scoped 404, successful delete, unknown-id 404.                                                                                                                                                                                                                                                                                                                                                  | ✅ Done — scoped-delete test                  |
| ✅ Email side-effect on `POST /api/leads` (`8bc719f`)                         | `api-wiring.test.ts` asserts `sendLeadNotificationEmail` is wired in the route and a mocked mailer is called per admin; live POST still creates the row.                                                                                                                                                                                                                                                                                       | ✅ Done — mocked mailer assertion             |
| ✅ AI contract path (`fae49dd`)                                               | `contract-ai.test.ts` imports `reviewContract()` with a stubbed `@anthropic-ai/sdk` (no live key): `method: "ai"` on valid JSON, heuristic fallback on throw / malformed / missing key.                                                                                                                                                                                                                                                        | ✅ Done — mocked SDK                          |
| ✅ Calendar endpoints (`0018c3e`)                                             | Covered in `api-wiring.test.ts` (Calendar block): auth-gate, unified range-GET shape, and the full manual-event POST→GET→PATCH→DELETE lifecycle (incl. namespaced `manual-<id>` lookup + 404-on-re-delete). Verifies the derived∪manual union and the authenticated write path against a real DB. **Remaining:** authenticated create still not live-tested on **prod** (no prod admin creds); prod deploy + auth-gate confirmed (401 unauth). | ✅ Done — added range-GET + CRUD assertions |
| ✅ Method-specific wiring (W8 leftovers)                                      | `api-wiring.test.ts` covers `PATCH /api/leads/bulk`, `POST /api/leads/:id/convert`, `POST /api/team/reorder`, `POST /api/notifications/broadcast`, and `/api/availability` GET/POST/DELETE.                                                                                                                                                                                                                                                    | ✅ Done — method coverage                     |

## P2 — Long-shot / parked

| Item                                     | Why parked                                                                                                                                                                                                                                                         |
| ---------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| **Hospital department data integration** | Prince flagged Apr 3: _"theres still sumthing big we can fix for them: their data kasi isnt integrated."_ Competitors cost tens of millions. ADVO doesn't have a wedge yet. Revisit only when there's a champion inside a hospital (David's Medical City cousin?). |
| **Daj**                                  | Wanted to bulk-submit media; no response since May. Don't chase — Prince's pivot: _"focusing on client generation rather than follow ups"_.                                                                                                                        |
| **Ms. Imee / Inventi**                   | Out of country, micromanages. Low-trust → low-throughput. Defer.                                                                                                                                                                                                   |

## Active client status

Detailed, because timelines + cash flow run through these:

### Felici Gelato — IN DEV, deadline pressure

- Owner: Reign (daughter). Mother runs the cafe. Portfolio of 3 businesses (cafe, flowers, gelato).
- ₱60k proposal + ₱3k/month maintenance. Hard deadline: June 12 (already past).
- Active concern (Jun 19, David): client requesting features she sees from competitor designers mid-build → scope creep.
- **Action:** finish Round 1, then enforce the new revision-limit clause for further work.

### Coffee Rush — IN DEV, no signed contract

- Owners: Tita Iya + Tito Adrian. Wants Zus-Coffee-style app, **no delivery** model.
- Juniors Anthony + Kenneth assigned.
- May 26 meeting at Eastwood happened. Tito said he'll refer other clients **once ADVO proves on Felici**.
- **Action:** sign contract before Round 1 ships. Block per "downpayment floor" rule above.

### FourlinQ — LARGEST OPEN DEAL (application contract, tier not yet selected)

The **website** shipped: `fourlinq.ph`, June 19, 2026. That engagement's post-mortem (₱12k downpayment insufficient, revisions unbounded → revenue gap) is still the case study driving the P0 contract changes below.

The **application** is a separate and much larger engagement, and it is open, not delivered. Per the contract dated **08/11/2026** (ADVO Web Development Services ⇄ FourlinQ Windows and Doors, "Integrated HR & Operations Management Application"), current as of **08/14/2026**:

| Term | What the contract says |
| --- | --- |
| **Investment** | Two tiers, one to be initialed by the client — **Tier 1: Core Attendance System ₱45,000**; **Tier 2: ADVANCED Integrated Management System ₱70,000** (Tier 1 features + leave management, company handbook, showroom QR tracking, full CRM/sales pipeline, designated website updates). Neither tier is initialed in the copy on file, so the tier is not yet selected. |
| **Payment** | 50% downpayment on signing with a witness present; 50% on final delivery + formal client sign-off (or deemed approval via 10-day non-response). Non-refundable once a milestone is signed off. Invoices payable within 7 business days; 2% per month penalty from the 16th business day. |
| **Recurring** | **₱3,000/month infrastructure fee**, billed on the 1st, starting upon final delivery and deployment (subject to final load testing). Covers Supabase-hosted infra, 99% uptime target, ~2–4GB RAM / ~100GB storage, hosting, DB maintenance, domain renewals. Non-payment for 15 days past due lets ADVO suspend hosting and API access. |
| **Timeline** | Estimated 6–8 weeks for Tier 1; 12–16 weeks for Tier 2. 30-day post-launch warranty. Client-caused delay extends the timeline day-for-day. |
| **Revisions** | Up to 5 rounds per deliverable, all usable until the "Project Sign-off" document is signed, and invokable for 6 months after signing if unused at final delivery. New modules or redesigns require a written change-order addendum. |
| **IP** | Source and deliverables remain ADVO's until full payment; ownership transfers on final payment. ADVO retains portfolio rights unless the client objects in writing before final delivery. |
| **Explicitly out of scope** | Wingman Tournament Software, Ticketless Parking System, and Real Estate / Fleet Management applications — the contract notes these were discussed but "require separate written addendums." |

**Capability gap — flag before signing.** The scope in this contract is materially beyond the platform ADVO ships today, which is websites + client hub + admin console. Nothing in the current codebase covers: biometric (FaceID/fingerprint) clock-in, offline-ready punch queuing and later sync, 200m-radius geofencing with real-time field-staff location tracking + map view + audit trail + manual off-site edit workflow, the 10-minute minimum dwell check-out rule, agency-vs-in-house account filtering, voice-to-text accomplishment reports, leave management with overlap flagging and paid-leave attachments, a digital company handbook with role-gated download, showroom QR walk-in tracking, or a CRM/sales pipeline with warranty and service orders. The attendance and field-tracking deliverables also imply a **native mobile client**, which ADVO has never shipped. This is stated as scope-vs-capability, not as a schedule or confidence claim; no delivery date is asserted here beyond the contract's own 6–8 / 12–16 week estimates.

### Medical City / Makati Med — PROSPECTING

- David's cousin (chief surgeon) is the in-road. No proposal sent yet.
- Idea floated: data-encoding service (lighter than the full hospital integration moonshot).

## May 26, 2026 meeting — decisions

3 guests + 5 ADVO members + Mr. Wagan. Priority order declared 3:47 PM:

1. **Felici Gelato** (first) — needs SocMed campaign for authentic Italian cafe + expansion to BGC/Makati
2. Flowers & Chocolates
3. Italian Cafe
4. Coffee Rush (lowest — most public, but lowest priority)

Other commitments from the meeting:

- **Franchise model** — private rollout first, public release after.
- **Long-term partnership** — Mr. Wagan signaled willingness to bring more clients post-Felici proof. "Considered retirement at 50."
- **Workload protection** — team studies stay priority; partners will throttle referrals accordingly.

## Honest gaps in this doc

- **Felici's exact open scope** — Round 1 revisions are still happening; no public list of what's left.
- **Coffee Rush feature list** — beyond "app like Zus, no delivery," details aren't in the chat archive I scanned.
- **Legal advisor** — Prince said one is needed; not booked.
- **FourlinQ application — tier not selected.** The copy on file (08/11/2026) has neither investment tier initialed, so the ₱45,000-vs-₱70,000 question is open. That copy is also truncated mid-clause in the liability section, so the signature page and anything after it are unverified here. Figures circulating verbally that differ from the ₱45,000 / ₱70,000 tiers and the ₱3,000/month infrastructure fee are **not** in the contract on file and are not recorded here.
- ~~**Email provider for lead notifications**~~ — ✅ resolved (`8bc719f`): the per-event template (`sendLeadNotificationEmail`) + trigger (`POST /api/leads`) now exist; sends via Resend when `RESEND_API_KEY` is set, else logs.
